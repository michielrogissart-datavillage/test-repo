description: Authenticated user does not have access
content:
  application/json:
    schema:
      type: object
      properties:
        error_message:
          type: string
      required:
        - error_message
